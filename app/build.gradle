apply plugin: 'com.android.application'

android {
    signingConfigs {
        balacsoft {
            keyAlias 'balacsoft'
            keyPassword 'abdullah2001'
            storeFile file('E:/Work-BAL/Android/Output_apk/_bulent.key')
            storePassword 'abdullah2001'
        }
    }
    compileSdkVersion 26
    buildToolsVersion "27.0.3"
    defaultConfig {
        applicationId "com.balacsoft.balacmemory"
        minSdkVersion 18
        targetSdkVersion 26
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
    implementation 'com.android.support:support-v13:26.1.0'
}
